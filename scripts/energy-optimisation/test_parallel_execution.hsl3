# def sendmmlcommand(command, select_from_column=0, *column_names, entry=0)
#     ClearMMLBuffer()
#     sendmml = SendMML(command)
#     rpt = GetMMLReport(0)
#     p = ParseMMLRpt(rpt)
#     getresult = GetResultCause(p)
#     col = []
#     if select_from_column == 1
#         for name in column_names
#             col.append(GetColumnByName(p, entry, name))
#         end
#         return p, rpt, getresult, col
#     end
 
#     return p, rpt, getresult
# end

ne = GetRegisterKey('sites')
Print(ne)
# ret = ConnectNE(site)
# if ret != 0
#     p, rpt, getresult, col = sendmmlcommand('LST CELL:;')
#     Print(rpt)

# end
Exit(0x10) 